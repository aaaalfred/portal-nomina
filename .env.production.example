# Production Environment - Dokploy Deploy
# Copiar este archivo como .env en Dokploy o configurar como Environment Variables

# Database
DATABASE_URL=postgresql://nomina_user:YOUR_DB_PASSWORD_HERE@postgres:5432/nomina
DB_PASSWORD=YOUR_DB_PASSWORD_HERE

# Redis
REDIS_URL=redis://redis:6379

# JWT - CRÍTICO: Generar con: openssl rand -base64 32
JWT_SECRET=GENERATE_A_STRONG_SECRET_HERE_WITH_OPENSSL
JWT_EXPIRES_IN=7d

# Storage (interno al container)
STORAGE_PATH=/app/storage

# Backend
PORT=3000
NODE_ENV=production

# Frontend API URL (debe apuntar al dominio de backend en producción)
VITE_API_URL=https://api.nomina.autia.com.mx
# O si usas proxy reverso: https://nomina.autia.com.mx/api

# Opcional: S3 Storage (si migras de local storage a S3)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=nomina-receipts

# Opcional: Email notifications (para futuros features)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=nomina@autia.com.mx
